arazzo: 1.0.1
info:
  title: Energy Data Exchange Workflow - Use Case 1 (AMI Meter Data)
  version: 1.0.0
  description: >
    End-to-end Beckn 2.0 workflow for inline AMI meter data exchange.
    The main delivery happens in on_confirm with DatasetItem dataPayload.

    Run via the workflow runner (from usecase1/workflows/):

      Local mode:
        ./run-arazzo.sh
        ./run-arazzo.sh -w select-through-confirm -v

      Over-internet mode:
        PUBLIC_URL=https://<host>.ngrok-free.dev ./run-arazzo.sh

      Direct respect invocation:
        npx @redocly/cli respect data-exchange.arazzo.yaml \
          --severity 'SCHEMA_CHECK=off' -v

sourceDescriptions:
  - name: beckn-bap-caller
    type: openapi
    url: https://raw.githubusercontent.com/beckn/protocol-specifications-v2/refs/tags/core-v2.0.0-lts/api/v2.0.0/beckn.yaml
    x-serverUrl: http://localhost:8081/bap/caller

  - name: beckn-bpp-caller
    type: openapi
    url: https://raw.githubusercontent.com/beckn/protocol-specifications-v2/refs/tags/core-v2.0.0-lts/api/v2.0.0/beckn.yaml
    x-serverUrl: http://localhost:8082/bpp/caller

workflows:
  - workflowId: publish-catalog
    summary: AMISP publishes AMI meter data catalog to catalog service
    steps:
      - stepId: publish-catalog
        description: AMISP publishes AMI meter data catalog with DatasetItem resources
        operationId: beckn-bpp-caller.catalogPublish
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/publish-catalog.json"

  - workflowId: discover
    summary: Discom discovers available AMI meter datasets via discover service
    steps:
      - stepId: discover
        description: BAP discovers available energy datasets on the network
        operationId: beckn-bap-caller.discover
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/discover-request.json"

  - workflowId: select-through-confirm
    summary: Discom selects, confirms, and receives AMI meter data inline on confirm
    steps:
      - stepId: select
        description: Discom selects AMI meter data dataset for inline delivery
        operationId: beckn-bap-caller.select
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/select-request.json"

      - stepId: on-select
        description: AMISP responds with terms for the selected dataset
        operationId: beckn-bpp-caller.onSelect
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/on-select-response.json"

      - stepId: init
        description: BAP provides billing details and buyer organization info
        operationId: beckn-bap-caller.init
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/init-request.json"

      - stepId: on-init
        description: BPP confirms the contract is ready for confirmation
        operationId: beckn-bpp-caller.onInit
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/on-init-response.json"

      - stepId: confirm
        description: BAP confirms the contract and expects inline delivery
        operationId: beckn-bap-caller.confirm
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/confirm-request.json"

      - stepId: on-confirm
        description: BPP confirms the contract and delivers AMI meter data inline
        operationId: beckn-bpp-caller.onConfirm
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/on-confirm-response.json"

  - workflowId: data-exchange-cancellation
    summary: Contract cancellation with refund
    steps:
      - stepId: cancel
        description: BAP cancels the data exchange contract
        operationId: beckn-bap-caller.cancel
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/cancel-request.json"

      - stepId: on-cancel
        description: BPP confirms cancellation with refund settlement
        operationId: beckn-bpp-caller.onCancel
        requestBody:
          contentType: application/json
          payload:
            $ref: "../examples/on-cancel-response.json"
