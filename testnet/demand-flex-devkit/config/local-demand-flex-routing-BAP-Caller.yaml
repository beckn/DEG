# BAP Caller routing: outbound requests from BAP → BPP adapter
routingRules:
  - version: "2.0.0"
    targetType: "bpp"
    endpoints:
      - select
      - init
      - confirm
      - status
      - update
      - cancel
